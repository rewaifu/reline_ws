[project]
name = "reline-websocet"
version = "0.1.1"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.129.0",
    "reline>=1.4.0",
    "uvicorn>=0.40.0",
]
